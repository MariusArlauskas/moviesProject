const state = {
  webTitle: "Movies Society"
};

const getters = {
  getWebTitle: state => state.webTitle
};

const actions = {};

const mutations = {};

export default {
  state,
  getters,
  mutations,
  actions
};
